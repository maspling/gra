# Example Configuration, copy this to config.toml and configure it to use.

[connect]
username = "" # Your RA Username
apiKey = "" # Your RA Api Key, retrieveable at https://retroachievements.org/settings
refreshInterval = 5 # Number of seconds between checking for achievement status.

[display]
achievementsPerRow = 8 # Achievements per row, raise and lower during runtime with keypad +/-